# Minimal Redis configuration for local development
# Persistence disabled for reduced IO in dev
save ""
appendonly no

# Memory limits (adjust as needed)
maxmemory 128mb
maxmemory-policy allkeys-lru

# Networking
protected-mode no
port 6379
bind 0.0.0.0


