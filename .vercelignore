# Exclude root api/ directory from Vercel deployment
# Using /api/ to explicitly match ONLY the root-level directory
# The api/ folder at apps/frontend/api/ contains serverless functions that MUST be deployed
/api/

# Also exclude backend and other non-production directories
apps/backend/
apps/functions-archive/
tests/
tools/
scripts/
docs/
docker/
oh-my-opencode/
